{{#if stats.failures}}
## ❌ Failed
{{else}}
## ✅ Passed
{{/if}}

✅ **{{stats.passes}} passed** ❌ **{{stats.failures}} failed** ❔ **{{stats.pending}} skipped** ⏱️ **{{ms stats.duration}}**

{{#if failures}}
<details>
  <summary>Failed tests</summary>
  {{#each failures as | test |}}
  Test: {{test.fullTitle}} <br>
  File: {{test.file}} <br>
  Duration: {{ms test.duration}} <br>
  <br>

  ```
  {{#if test.err.stack}}
  {{{test.err.stack}}}
  {{else}}
  {{{test.err.message}}}
  {{/if}}
  ```

  ---
  {{/each}}
</details>
{{/if}}

